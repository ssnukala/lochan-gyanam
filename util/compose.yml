services:
  dev:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vardhan-dev
    stdin_open: true
    tty: true
    working_dir: /gyanam
    env_file:
      - .env
    volumes:
      - ..:/gyanam
      - ../../:/docker
    network_mode: host
    command: bash
