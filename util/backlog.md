# util/ Script Backlog

Missing scripts and daksh command gaps. When a session needs a Docker operation that has no daksh/util equivalent, document it here instead of using raw Docker commands.

## Format

```
### <script-name>.sh
**Need:** What it should do
**Params:** What parameters it takes
**Workaround:** What raw Docker command people are currently using
**Priority:** P0 (blocking) / P1 (frequent) / P2 (nice to have)
```

---

## Backlog

### restart-app.sh — DONE
### logs-app.sh — DONE
### status-apps.sh — DONE

### exec-backend.sh — DONE
### seed-app.sh — DONE
### rebuild-frontend.sh — DONE
